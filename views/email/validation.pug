extends baseEmail 

block content

    p Hi #{firstName},
    p Almost there! Next step is to complete the registration process by submitting a PATCH request to: #{url}
    p (Website for this action not yet implemented.)
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td
                                    a(href=`${url}` target='_blank') Confirm your signup